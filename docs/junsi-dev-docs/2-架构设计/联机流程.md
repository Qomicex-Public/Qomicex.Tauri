# 联机流程（Connector）

## 概述

Qomicex Launcher 的联机系统基于 **EasyTier**（P2P VPN）构建虚拟局域网，在此之上运行 **Scaffolding 协议**来管理房间和玩家。核心思路：用 EasyTier 将房主和房客置于同一虚拟子网，然后通过端口转发让房客连接到房主的 Minecraft 服务器。

## 架构总览

| 层 | 位置 | 职责 |
|:---|:---|:---|
| API 端点层 | `Endpoints/ConnectorEndpoints.cs` | 暴露 9 个 REST 端点 |
| 服务编排层 | `Services/Connector/ConnectorService.cs` | 协调子组件，管理会话生命周期 |
| Scaffolding 核心库 | `Qomicex.Connector/` | 房间创建/加入、协议通信、EasyTier 管理 |

## 网络拓扑

```
 ┌─────────────┐        ┌─────────────┐
 │   房主       │        │   房客 A     │
 │ 10.144.144.1 │        │  DHCP 分配   │
 │ MC:25565     │◄──────►│              │
 │ Center:xxxxx │  EasyTier P2P/Relay  │
 └─────────────┘        └─────────────┘
       │                       │
       │     ┌───────────┐     │
       └─────┤ 中继节点   ├─────┘
             │ (可选回退) │
             └───────────┘
```

---

## 房主创建房间

### 模式A：已知端口（`POST /api/connector/host/port`）

```
用户输入端口 / 系统自动扫描端口
  │
  ▼
GameProcessInspector.Inspect(port)
  ├── FindPidByPort(port) → PID
  ├── GetCommandLine(pid) → 启动参数
  └── 提取 --username, --uuid, --userType, --version
  │
  ▼
RoomCode.Generate() → "U/XXXX-XXXX-XXXX-XXXX"
  │
  ▼
启动 ScaffoldingCenter（在空闲 TCP 端口上）
  │
  ▼
启动 EasyTier 进程
  配置：--ipv4 10.144.144.1
       --network-name scaffolding-mc-{房间码前8位}
       --network-secret {房间码后8位}
       --tcp-whitelist={Center端口},{MC端口}
  │
  ▼
返回房间码给房客
```

### 模式B：选实例启动（`POST /api/connector/host/instance`）

自动启动游戏 + 自动发现端口：

1. 调用 `/api/instance/{id}/launch` 启动实例
2. 循环等待（最长5分钟，每2秒检测）：
   - **LAN 广播检测**：`LanGameListenerService` 监听 `224.0.2.60:4445` 组播
   - **TCP 端口扫描**：`GameProcessInspector.ScanAllJavaPorts()`
3. 发现新端口 → 进入模式A流程

---

## 房客加入房间（`POST /api/connector/join`）

```
输入房间码
  │
  ▼
RoomCode.Parse(code)
  │
  ▼
创建 ScaffoldingGuest
  │
  ▼
启动 EasyTier（房客配置：--no-tun --use-smoltcp --dhcp）
加入同一网络（--network-name / --network-secret）
  │
  ▼
CenterDiscoveryService.DiscoverAsync()
  在 EasyTier 网络中查找 hostname 匹配 ^scaffolding-mc-server-(\d+)$ 的节点
  最多等待30秒（每0.5秒扫描）
  │
  ├── 直连模式（优先）：TCP 连接 Center 虚拟IP:端口
  └── 端口转发模式（回退）：--port-forward tcp://127.0.0.1:{本地}/{中心IP}:{中心端口}
  │
  ▼
通过 c:server_port 协议获取 Minecraft 端口
通过 MapMinecraftPortAsync 映射端口到本机
  │
  ▼
返回 (mcHost, mcPort) → 前端写入 Minecraft 多人游戏地址
```

---

## Scaffolding 协议

在 TCP 上运行的二进制协议框架。

### 帧格式

```
┌──────────────────┬────────────────┬──────────┬──────────┐
│ body_len (4B BE) │ namespace(str) │ type(str)│   body   │
└──────────────────┴────────────────┴──────────┴──────────┘
```

### 标准协议（`c:` 命名空间）

| 协议键 | 用途 | 方向 |
|:---|:---|:---|
| `c:ping` | 心跳检测 | 双向 |
| `c:protocols` | 协议协商 | Guest→Center |
| `c:server_port` | 获取 MC 端口 | Guest→Center |
| `c:player_ping` | 玩家心跳（含名称、MachineId、EasyTierId） | Guest→Center |
| `c:player_profiles_list` | 获取在线玩家列表 | Guest→Center |

### QML 扩展协议（`qml:` 命名空间）

| 协议键 | 用途 | 方向 |
|:---|:---|:---|
| `qml:game_info` | 交换游戏版本信息 | Guest→Center |
| `qml:player_icons` | 交换玩家头像（Base64 PNG） | 双向 |
| `qml:player_leave` | 优雅退出通知 | Guest→Center |

### 心跳机制

每 **5 秒**发送一次 `c:player_ping`，连续失败触发 `ConnectionLost` → 自动调用 `LeaveAsync()`。

---

## EasyTier 配置详解

| 配置项 | 房主（Center） | 房客（Guest） |
|:---|:---|:---|
| `--ipv4` | `10.144.144.1`（固定） | 不设（DHCP） |
| `--no-tun` | 否 | 是 |
| `--use-smoltcp` | 否 | 是（用户态网络栈） |
| `--dhcp` | 否 | 是 |
| `--tcp-whitelist` | Center端口 + MC端口 | 无 |
| `--port-forward` | 无 | 远端端口映射到 127.0.0.1 |

### 关键技术点

- **P2P 打洞**：房客最多重试 **10 次**（间隔2秒）
- **端口转发回退**：房客无 TUN 设备权限时回退到 `--port-forward`
- **中继节点**：P2P 打洞失败时流量通过中继转发

---

## EasyTier 下载管理

**文件：** `Services/Connector/EasyTierProvider.cs`

- **版本**：v2.6.4
- **安装路径**：`{AppPaths.BaseDir}/QML/EasyTier/`
- **状态流转**：`idle → resolving → downloading → extracting → installed`（失败则 `failed`）
- **镜像测速**：并发探测 6 个代理前缀，选最快的
- **平台支持**：Windows / macOS / Linux（x86_64 / aarch64 / i686）

---

## GameProcessInspector

从 TCP 端口反查 Minecraft 进程信息，不直接挂钩进程，仅通过 OS API 读取进程元数据。

| 步骤 | Windows | Linux | macOS |
|:---|:---|:---|:---|
| 端口→PID | `GetExtendedTcpTable` (P/Invoke) | 读 `/proc/net/tcp` + inode 反查 | `lsof` |
| PID→命令行 | WMI / `wmic` / PowerShell | 读 `/proc/{pid}/cmdline` | `ps -p` |
| 参数解析 | 提取 `--username`, `--uuid`, `--userType`, `--version` | 同 | 同 |

---

## LanGameListenerService

监听 Minecraft 的 **LAN 广播**（Minecraft"对局域网开放"时发送）。

- **组播地址**：`224.0.2.60:4445`
- **协议**：UDP
- **消息格式**：`[MOTD]服务器名称[/MOTD][AD]端口号[/AD]`
- **条目存活期**：30秒无更新自动清理

在"选实例启动"模式中，用户需在游戏内点击"对局域网开放"→ 系统收到广播 → 自动触发创建房间。

---

## 房间码格式

`RoomCode.Generate()` 生成格式：`U/XXXX-XXXX-XXXX-XXXX`

- base-34 编码，含校验和
- 前8位作为 EasyTier 网络名，后8位作为网络密钥
- 校验和防止输入错误

---

## 中继节点系统

```
私有节点（PrivateRelayNodeFetcher）→ 公开节点API（nodes.qomicex.top）→ 内置默认节点
```

**节点 API**：`https://nodes.qomicex.top/api/nodes`，按系统区域优先匹配同区域节点。

**默认回退**：`etnode.zkitefly.eu.org`

---

## NAT 类型检测

`NatTypeDetector` 使用 STUN 协议检测：

| 结果 | 含义 |
|:---|:---|
| `cone` | 全锥形 NAT，P2P 成功率高 |
| `symmetric` | 对称 NAT，打洞困难 |
| `blocked` | UDP 阻断 |
| `unknown` | 仅测通一个服务器 |

前端 Badge：宽松（cone）/ 严格（symmetric，destructive 红色）

---

## 前端联机页面（`src/pages/Connect.tsx`）

**UI 结构**：

- **顶部**：NAT 类型 Badge + "NAT 检测"按钮
- **EasyTier 下载卡片**：进度条、速度、状态、失败重试
- **空闲状态**：
  - 创建房间 Tab：选实例启动 / 扫描端口 / 手动输入端口
  - 加入房间 Tab：输入房间码 `U/XXXX-XXXX-XXXX-XXXX`
- **房主视图**：房间码（可复制）+ 玩家列表 + 关闭按钮
- **房客视图**：服务器地址 `host:port`（可复制）+ 版本信息 + 玩家列表 + 退出按钮

**轮询**：`status.mode !== 'idle'` 时每2秒轮询 `/api/connector/status`

---

## 关键文件

| 层 | 文件 | 职责 |
|:---|:---|:---|
| 前端 | `src/pages/Connect.tsx` | 联机页面 UI |
| 前端 | `src/api/connector.ts` | 联机 API 封装 |
| 后端 | `Endpoints/ConnectorEndpoints.cs` | 9 个 REST 端点 |
| 后端 | `Services/Connector/ConnectorService.cs` | 核心服务：协调中心/房客生命周期 |
| 后端 | `Services/Connector/GameProcessInspector.cs` | 端口→PID→命令行解析 |
| 后端 | `Services/Connector/EasyTierProvider.cs` | EasyTier 下载/安装管理 |
| 后端 | `Services/Connector/NatTypeDetector.cs` | STUN NAT 类型检测 |
| 后端 | `Services/LanGameListenerService.cs` | LAN 组播监听 |
| 核心库 | `Scaffolding/Center/ScaffoldingCenter.cs` | 房主侧联机中心 |
| 核心库 | `Scaffolding/Guest/ScaffoldingGuest.cs` | 房客侧联机客户端 |
| 核心库 | `Scaffolding/EasyTierManager.cs` | EasyTier 进程生命周期 |
| 核心库 | `Scaffolding/Core/CenterDiscoveryService.cs` | 网络中的中心发现 |
| 核心库 | `Scaffolding/Core/HeartbeatService.cs` | 心跳服务 |
| 核心库 | `Scaffolding/Models/RoomCode.cs` | 房间码生成/解析/校验 |




### 2026-08-12 更新
> **修订（2026-08-12）—— Rust 实现的行为差异（`src-backend/qomicex-backend/src/endpoints/connector.rs` `host_instance`）：**

### 模式B：选实例启动（Rust 实现）

1. 后台任务直接 `core.launch().launch()`（不走 `/api/instance/{id}/launch`），进度写入 `LaunchTracker`（`starting`→`running(100%)` 表示游戏已启动、正在检测端口），前端 Connect 页轮询 `/api/instance/{id}/launch/progress` 显示步骤与失败原因。
2. 端口检测：TCP 端口扫描（`scan_java_listening_ports`）最长 **60s**（原 C# 为 5 分钟 + LAN 组播广播，Rust 仅扫描）。
3. 失败语义（前端不再卡等待页）：
   - 启动失败 → 立即写 `failed` + `mode=idle` 复位（不白等 60s）
   - 60s 未检测到端口 / 建房失败 → 写 `failed` + `mode=idle`
   - 前端 `isStarting` 卡片渲染 `failed.error` 为常驻错误卡片（「知道了」关闭）
4. 取消语义：Starting 阶段调 `/api/connector/leave` 会 `LaunchTracker.stop(instanceId)` 杀游戏进程 + 置取消信号，后台轮询/建房流程随即放弃（杜绝 leave 后残留建房）。
5. 建房成功后 `LaunchTracker` 保留 `running` 进度（游戏仍在运行），进程退出后经 launch/progress 结算游玩时长。



### 2026-08-12 更新
> **修订（2026-08-12）—— 联机/实例启动失败 `os error 123` 根因与修复：**

### 现象
- 启动（联机"启动并创建房间"或实例列表启动）报 `invalid params: 启动失败,文件名、目录名或卷标语法不正确 (os error 123)`；或前端卡"准备启动"、15s 请求超时、启动 dialog 关不掉。

### 根因链（已实测定位）
1. **connector 的 `build_launch_options` 未设 `game_root`**：core 的 `ProcessLaunchExecutor` 用 builder 默认 game_dir（Windows 长路径规范化为 `\\?\` verbatim 前缀）。`unzip_natives` 的 natives_dir 随之带 `\\?\` 前缀，而版本 JSON 模板 `-Djava.library.path=${natives_directory}/java` 用 `/` 拼接——**verbatim 语法下 `/` 不是分隔符**，`...26.2-natives/java` 被当作含 `/` 的非法文件名 → `create_dir_all` 报 `ERROR_INVALID_NAME (123)`（Backtrace 定位 process.rs:350）。
2. **core `normalize_arg` 给含空格 Java 路径加引号**：`Command::new("\"C:/Program Files/.../java.exe\"")` 在 Windows 上 CreateProcessW 不剥引号 → spawn 失败（os error 123）。Rust 不模拟源 C# CreateProcess 的剥引号行为。
3. **connector 路径 `auth_options: None`**：`--accessToken` 空 → joptsimple `Missing required option(s) [accessToken, version]`，游戏进程立即退出。
4. **前端 dialog 残留**：`RunningContext.launchInstance` 的 `apiLaunchInstance` 抛错（超时）时无 catch → `launchProgress` 停在"准备启动"，非 final 阶段 dialog 无关闭按钮 → 关不掉。

### 修复
- **backend `connector.rs`**：`build_launch_options` 补 `game_root: Some(instance.game_dir)`（与普通启动一致）；Java 路径复用 `instance::resolve_java_path`（用户指定优先，否则 scan_quick+recommand 自动推荐——此前所有 javaPath 为空的实例必然失败）；auth 复用 `instance::resolve_auth_options`（默认账号）；core.launch() 执行期间每 2s 心跳进度（`launching` 50%），失败写 `failed` 并立即复位 Idle（不白等 60s）。
- **core（qomicex-core-rust submodule）`process.rs`**：① `file_name` 统一 `trim_matches('"')` 去引号；② `parse_java_library_path` 对 `\\?\` verbatim 路径把 `/` 换 `\`（防御）。
- **前端**：`RunningContext.launchInstance` 的 `apiLaunchInstance` 包 try/catch（失败清 dialog + 显示错误）；`cancelLaunch` 先清 UI 再调后端（取消 API 挂起时 dialog 也能关）；`LaunchProgressDialog` 非 final 阶段加"取消"按钮。



### 2026-08-12 更新
> **修订（2026-08-12）—— 房主踢人：**

### 踢人流程（`POST /api/connector/kick`，仅房主）

```
房主玩家列表 [踢出 ✕]  ← 点击（自己那行不显示）
  ▼
POST /api/connector/kick { machineId }
  ▼ ScaffoldingCenter::kick_player（三层）
  ① easytier: disconnect_peer(easytier_id)     —— 非 QML SCF 客户端仅此手段
  ② Scaffolding TCP: ClientRegistry 定向断开   —— QML guest 心跳失败 → 整体退出
  ③ remove_player + icon_map 清理
```

**为什么需要 easytier 层**：其他启动器的 SCF 客户端连 QML 房间时不受 `qml:player_leave` 等 QML 专属协议控制，只能从 easytier 层物理断开（fork 新增 `Easytier::disconnect_peer`，底层 `PeerManager::close_peer` 关闭全部连接）。

**已知限制**：287c667 fork 无 peer 级控制面 deny，非 QML 客户端被踢后若持续在线，easytier 可能自动重连（ACL 仅数据面过滤）。

### 2026-08-14 修订 —— 踢人流程升级（非 QML guest 可踢除）

> 取代上文「踢人流程（三层）」：`kick_player` 现为**断开 + 黑名单**四步：

```
POST /api/connector/kick { machineId }
  ▼ ScaffoldingCenter::kick_player
  ① easytier peer 解析 + disconnect_peer
      优先已上报 easytier_id；否则 hostname（scaffolding-mc-guest-{machine_id前8位}）
      或 SCF TCP 源虚拟 IP（machine_source_ip → get_nodes() 反查）定位 peer
  ② 记入已踢黑名单（含解析到的 peer id）
  ③ Scaffolding TCP: ClientRegistry 定向断开（找不到连接打 warn）
  ④ remove_player + icon_map 清理
被踢 guest 再发 c:player_ping → 拒绝入列 + 状态 255（不刷新心跳 → 15s 兜底剔除）+ 重复断开
```

**为什么需要 easytier 层**：其他启动器的 SCF 客户端连 QML 房间时不受 `qml:player_leave` 等 QML 专属协议控制，只能从 easytier 层物理断开（fork 新增 `Easytier::disconnect_peer`，底层 `PeerManager::close_peer` 关闭全部连接）。

**已知限制（更新）**：fork 无 peer 级控制面 deny，被踢 guest 的 easytier 连接仍会自动重连；黑名单在 SCF 层拦截其重新入列并每次 ping 重复断开 peer（数据面封禁不彻底）。彻底方案需给 fork 加控制面 deny（待办）。



### 2026-08-12 更新
| API 端点层 | `src-backend/qomicex-backend/src/endpoints/connector.rs` | 暴露 10 个 REST 端点（host/port、host/instance、join、status、match-instances、leave、**kick**、easytier/status、easytier/download、scan-ports、nat-type） |
| 服务编排层 | `Services/Connector/ConnectorService.cs` | 协调子组件，管理会话生命周期 |
| Scaffolding 核心库 | `Qomicex.Connector/`（Rust：`qomicex-connector-rust`） | 房间创建/加入、协议通信、EasyTier 管理 |

> Rust 实现由 `qomicex-connector-rust`（含 easytier fork `EasyTier4QML` rev 8fccc86）提供：`ScaffoldingCenter::kick_player` 三层踢出（et 全断连接 + Scaffolding TCP 定向断开 + 玩家列表移除）。



### 2026-08-12 更新
| API 端点层 | `src-backend/qomicex-backend/src/endpoints/connector.rs` | 暴露 11 个 REST 端点（host/port、host/instance、join、status、match-instances、leave、**kick**、easytier/status、easytier/download、scan-ports、nat-type） |
| 服务编排层 | `Services/Connector/ConnectorService.cs` | 协调子组件，管理会话生命周期 |
| Scaffolding 核心库 | `Qomicex.Connector/`（Rust：`qomicex-connector-rust`） | 房间创建/加入、协议通信、EasyTier 管理 |

> Rust 实现由 `qomicex-connector-rust`（含 easytier fork `EasyTier4QML` rev 8fccc86）提供：`ScaffoldingCenter::kick_player` 三层踢出（et 全断连接 + Scaffolding TCP 定向断开 + 玩家列表移除）。



### 2026-08-12 更新


### 2026-08-12 修订 —— 联机 bug 修复

- **自踢防护（后端权威校验）**：`/connector/kick` 的自踢校验不再依赖重算 `machine_code()`（`getmac` 首个有效 MAC，VPN 网卡顺序变化时可漂移），改以 center 玩家列表 `kind==Host` 的 `machine_id` 为准（保留机器码比对兜底）。前端 `PlayerList` 房主行不再渲染踢出按钮（原实现只守卫点击、按钮仍显示，与"自己那行不显示"的意图不符）。
- **原版房主匹配语义**：`/connector/match-instances` 中房主发布 0 个 mods（原版/未发布）时，原逻辑 `!room_hashes.is_empty() && is_subset` 使任何实例都匹配不上 → guest 原版实例永远"mods 不一致"无法快捷启动。改为：房主 0 mods 时仅当本地实例也为 0 mods 才算一致（原版-原版匹配；原版房主 + 有 mods 的 guest 仍不匹配）。
- **联机中心启动的游戏接入"运行中的游戏"**：`RunningContext` 新增 `watchInstance(id, name)`（复用 `startPoll` 轮询，不发起启动）。房主 `host/instance` 成功后由后端代启，前端经 `watchInstance` 注册进运行列表；guest 快捷启动改用 `useRunning().launchInstance`（顺带获得崩溃弹窗与启动/退出通知）。

### 2026-08-12 修订 —— 缺失 mod 标记

- **问题**：join 房间后房主 mods 正常拉取，但本地实例因缺 mod 而"不匹配"，宿主 Mods 列表却无法提示具体缺哪个 mod。
- **修复（后端）**：`/connector/match-instances` 响应新增 `missingHashes`（`Vec<String>`，缺失房主 mod 的 sha1，按房主 mods 顺序）与 `referenceInstance`（`Option<String>`，作为缺失判定参考的本地实例名）。参考实例 = **覆盖房主 mods（sha1 命中）最多的本地同版本实例**；无同版本实例时（`instances` 为空）房主全部 mods 均视为缺失。内部：`MatchedInstance` 新增 `#[serde(skip)] local_hashes` 承载各实例 mod sha1 集合以便后端比对，不下发给前端。
- **修复（前端）**：`Connect.tsx` 的 `RoomModsCard` 房主 Mods 列表对命中 `missingHashes` 的 mod 渲染红色"缺失"徽标（Tooltip 说明参考实例）；区块标题显示参考实例名并附说明文字。
- **忽略差异强制启动**：`RoomModsCard` 的"不匹配"实例行新增**"忽略差异强制启动"**按钮——同版本/loader 下仅 mod 集不完全一致（缺失部分 mod）时，仍可忽略差异启动该实例加入房间（走同一 `handleQuickLaunch`/`joinServer`）。`match_instances` 候选已按房主版本/loader 预筛，差异仅指 mod 缺失，强制启动安全。
- **页签折叠**：`RoomModsCard` 改为 `Tabs`/`TabContent` 页签折叠。guest 侧含 **玩家列表 / 房主 Mods / 匹配实例** 三页签，**默认选中玩家列表**（进房先看房内成员）；匹配/缺失/忽略差异启动内容收敛进各页签，避免堆叠杂乱。扫描进行中时页签内显示加载提示。



### 2026-08-12 更新


### 2026-08-12 修订 —— guest 被踢自动退出房间

- **问题**：房主踢出 guest 后，guest 端 `status` 恒返回 `mode=guest`（TCP/easytier 已断但模式未复位），前端卡在"还在房间里"，玩家列表为空。
- **修复**：`join_room` 成功后 spawn 监听 `guest.connection_lost_rx()`（心跳/发送失败时置 true）；触发后 `reset_connector_state(false)` 复位 Idle（close_all + 清缓存）。提取公共 `reset_connector_state(notify_leave)`：手动 `leave` 传 true（发 qml:player_leave 优雅通知），连接丢失传 false（TCP 已断，避免 15s 读超时拖慢清理）。监听任务仅持 watch Receiver 不持 guest Arc——手动 leave 后 guest drop → channel 关闭 → 任务自动退出，无残留。

